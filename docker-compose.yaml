services:
  frontend:
    build: ./design
    ports:
      - "8080:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb+srv://user:pass@dockerr.su6cgfr.mongodb.net/
      - PORT=5000
